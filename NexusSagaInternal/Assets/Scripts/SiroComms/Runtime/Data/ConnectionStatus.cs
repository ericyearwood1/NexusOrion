namespace SiroComms.Runtime.Data
{
    public enum ConnectionStatus
    {
        None,
        NotConnected,
        Connecting,
        Connected,
        Disconnecting,
        ConnectionError
    }
}